import * as React from 'react';
import { View, StyleSheet } from 'react-native';

export default function ProductHeader({ image }) {
  console.log(image);

  return (
    <View>

    </View>
  );
}

const styles = StyleSheet.create({
  
});
